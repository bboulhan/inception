version: "3.8"
services:
  # nginx:
  #   build: ./requirements/nginx
  #   container_name: nginx_c
  #   ports:
  #     - "443:443"

  mariadb:
    build: ./requirements/mariadb
    container_name: mariadb_c
    env_file:
      - .env
    ports:
      - "3306:3306"
    volumes:
      - ./mysql:/var/lib/mysql
    stdin_open: true
    tty: true

    